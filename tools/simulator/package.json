{
  "name": "@webhook-monitor/simulator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run --root ../.. tools/simulator/test",
    "typecheck": "tsc -b ../../tsconfig.json --pretty false"
  },
  "dependencies": {
    "@webhook-monitor/core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  }
}
